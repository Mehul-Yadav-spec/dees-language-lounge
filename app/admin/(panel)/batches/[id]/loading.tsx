import { PortalSkeleton } from "@/components/portal/PortalSkeleton";

// Dedicated skeleton for the (data-heavy) admin batch detail page.
export default function Loading() {
  return <PortalSkeleton />;
}
