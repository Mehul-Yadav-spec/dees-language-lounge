import { PortalSkeleton } from "@/components/portal/PortalSkeleton";

// Dedicated skeleton for the (data-heavy) tutor batch detail page.
export default function Loading() {
  return <PortalSkeleton />;
}
